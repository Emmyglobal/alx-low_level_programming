#include "main.h"

/**
 * add -> Entry point
 * @first: first number
 * @second: the second number
 *
 * Return: Always 0
 */
int add(int first, int second)
{
	int sum;

	sum = first + second;
	return (sum);
}
